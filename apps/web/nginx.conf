# apps/web/nginx.conf — needed so React Router's client-side routes don't 404 on refresh
server {
    listen 80;
    location / {
        root /usr/share/nginx/html;
        try_files $uri $uri/ /index.html;
    }
}